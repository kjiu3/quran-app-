# 🧪 دليل الاختبارات - تطبيق القرآن الكريم

## 📋 نظرة عامة

هذا المجلد يحتوي على جميع الاختبارات للتطبيق الإسلامي. الاختبارات مقسمة إلى فئات لتسهيل الصيانة والتطوير.

---

## 📁 هيكل المجلد

```
test/
├── providers/              # اختبارات للمزودات (Providers)
│   ├── audio_provider_test.dart
│   └── theme_provider_test.dart
├── widgets/                # اختبارات للعناصر المرئية (Widgets)
│   ├── error_handler_widget_test.dart
│   ├── feature_card_test.dart
│   └── smart_image_test.dart
└── README.md              # هذا الملف
```

---

## 🧪 أنواع الاختبارات

### 1. اختبارات المزودات (Provider Tests)

#### `audio_provider_test.dart`
اختبارات شاملة لـ `AudioProvider` تشمل:
- ✅ الحالة الأولية للمزود
- ✅ إدارة الحالة (State Management)
- ✅ معالجة الأخطاء
- ✅ إدارة المستمعين (Listeners)
- ✅ إدارة الموارد (Resource Management)

**عدد الاختبارات:** 13 اختبار

#### `theme_provider_test.dart`
اختبارات لـ `ThemeProvider` تشمل:
- ✅ تبديل الوضع الليلي/النهاري
- ✅ حفظ التفضيلات
- ✅ تحميل التفضيلات

---

### 2. اختبارات العناصر المرئية (Widget Tests)

#### `error_handler_widget_test.dart`
اختبارات لـ `ErrorHandlerWidget` و `BufferingIndicator`:
- ✅ عرض رسائل الخطأ
- ✅ أزرار إعادة المحاولة والإغلاق
- ✅ معالجة الحالات الخاصة
- ✅ مؤشر التحميل (Buffering)

**عدد الاختبارات:** 13 اختبار

#### `feature_card_test.dart`
اختبارات لـ `FeatureCard`:
- ✅ عرض العنوان والأيقونة
- ✅ التنقل عند النقر
- ✅ معالجة الصور المختلفة
- ✅ التفاعل مع المستخدم
- ✅ إمكانية الوصول (Accessibility)

**عدد الاختبارات:** 12 اختبار

#### `smart_image_test.dart`
اختبارات لـ `SmartImage`:
- ✅ عرض الصور المحلية
- ✅ عرض الصور من الإنترنت
- ✅ الأيقونات البديلة (Fallback Icons)
- ✅ معالجة الأخطاء
- ✅ الحالات الخاصة

**عدد الاختبارات:** 8 اختبارات

---

## 🚀 تشغيل الاختبارات

### تشغيل جميع الاختبارات
```bash
flutter test
```

### تشغيل اختبارات محددة
```bash
# اختبارات المزودات فقط
flutter test test/providers/

# اختبارات العناصر المرئية فقط
flutter test test/widgets/

# اختبار ملف محدد
flutter test test/providers/audio_provider_test.dart
```

### تشغيل مع تقرير التغطية
```bash
flutter test --coverage
```

---

## 📊 إحصائيات الاختبارات

### الاختبارات الحالية
- **اختبارات المزودات:** 15+ اختبار
- **اختبارات العناصر المرئية:** 33+ اختبار
- **إجمالي الاختبارات:** 48+ اختبار

### حالة التغطية
- ✅ **AudioProvider:** تغطية شاملة للحالات الأساسية
- ✅ **ThemeProvider:** تغطية كاملة
- ✅ **ErrorHandlerWidget:** تغطية شاملة
- ✅ **FeatureCard:** تغطية شاملة
- ✅ **SmartImage:** تغطية شاملة

---

## 🔮 الاختبارات المستقبلية

### الأولوية العالية 🔴
- [ ] اختبارات متقدمة لـ AudioProvider مع mocking
- [ ] اختبارات للشاشات الرئيسية
- [ ] اختبارات التكامل (Integration Tests)

### الأولوية المتوسطة 🟡
- [ ] اختبارات الأداء (Performance Tests)
- [ ] اختبارات الذاكرة (Memory Tests)
- [ ] اختبارات إمكانية الوصول (Accessibility Tests)

### الأولوية المنخفضة 🟢
- [ ] اختبارات E2E (End-to-End)
- [ ] اختبارات الأمان (Security Tests)

---

## 📝 إرشادات كتابة الاختبارات

### 1. تسمية الاختبارات
```dart
// ✅ جيد - وصف واضح
test('should display error message when provided', () { ... });

// ❌ سيء - غير واضح
test('test1', () { ... });
```

### 2. تنظيم الاختبارات
```dart
group('FeatureName - Category', () {
  setUp(() {
    // إعداد قبل كل اختبار
  });

  tearDown(() {
    // تنظيف بعد كل اختبار
  });

  test('should do something specific', () {
    // الاختبار
  });
});
```

### 3. استخدام Matchers المناسبة
```dart
// ✅ استخدام matchers واضحة
expect(find.text('القرآن'), findsOneWidget);
expect(audioProvider.isPlaying, false);
expect(count, greaterThan(0));

// ❌ تجنب المقارنات المباشرة غير الواضحة
expect(result == true, true);
```

### 4. اختبار الحالات الخاصة
```dart
// اختبار القيم الفارغة
test('should handle null values', () { ... });

// اختبار القيم الطويلة
test('should handle very long strings', () { ... });

// اختبار الأخطاء
test('should handle errors gracefully', () { ... });
```

---

## 🛠️ أدوات الاختبار

### المكتبات المستخدمة
- `flutter_test` - إطار الاختبار الأساسي
- `mockito` - لإنشاء mock objects (مخطط للمستقبل)
- `build_runner` - لتوليد الكود (مخطط للمستقبل)

### أوامر مفيدة
```bash
# تشغيل الاختبارات مع وضع verbose
flutter test --verbose

# تشغيل الاختبارات مع تقرير مفصل
flutter test --reporter expanded

# تشغيل اختبار محدد
flutter test test/providers/audio_provider_test.dart

# تحديث الحزم
flutter pub get
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: الاختبارات لا تعمل
```bash
# حل 1: تحديث الحزم
flutter pub get

# حل 2: تنظيف المشروع
flutter clean
flutter pub get

# حل 3: التحقق من إصدار Flutter
flutter --version
```

### مشكلة: اختبارات Widget تفشل
```dart
// تأكد من استخدام MaterialApp
await tester.pumpWidget(
  MaterialApp(
    home: Scaffold(
      body: YourWidget(),
    ),
  ),
);

// استخدم pumpAndSettle للانتظار حتى انتهاء الرسوم المتحركة
await tester.pumpAndSettle();
```

---

## 📚 موارد إضافية

### التوثيق الرسمي
- [Flutter Testing Documentation](https://flutter.dev/docs/testing)
- [Widget Testing](https://flutter.dev/docs/cookbook/testing/widget/introduction)
- [Integration Testing](https://flutter.dev/docs/testing/integration-tests)

### أفضل الممارسات
- [Testing Best Practices](https://flutter.dev/docs/testing/best-practices)
- [Mockito Documentation](https://pub.dev/packages/mockito)

---

## ✅ قائمة التحقق قبل الإضافة

قبل إضافة اختبار جديد، تأكد من:
- [ ] الاختبار له اسم واضح وموصوف
- [ ] الاختبار مستقل ولا يعتمد على اختبارات أخرى
- [ ] استخدام `setUp` و `tearDown` بشكل صحيح
- [ ] اختبار الحالات الطبيعية والخاصة
- [ ] التعليقات واضحة عند الحاجة
- [ ] الاختبار يمر بنجاح

---

## 🎯 الهدف

**الوصول إلى تغطية 80%+ من الكود بالاختبارات**

الحالة الحالية: **~60%** (تقديري)

---

*آخر تحديث: 2025-11-24*
*المطور: فريق تطبيق القرآن الكريم*
